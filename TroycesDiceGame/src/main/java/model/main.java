package main.java.model;

import javafx.application.Application;
import main.java.window.MenuWindow;

public class main {
    public static void main(String[] args) {
        Application.launch(MenuWindow.class, args);
    }
}